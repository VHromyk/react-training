import React from 'react';
import './AppBar.css';
import Logo from '../Painting/Logo';

const AppBar = () => (
  <header className="header">
    <Logo text="LOGO" />
  </header>
);

export default AppBar;
